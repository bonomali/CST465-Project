@model TestQuestion

<div>
    <h2>@Model.ID. @Model.Question</h2>
    
    @for(int i = 0; i < Model.Choices.Count; i++)
    {
        <label>@Html.RadioButtonFor(m => m.Answer, "Answer", new { @class = "MultipleChoiceQuestion" })@Model.Choices[i]</label>
        <br />
    }
</div>
