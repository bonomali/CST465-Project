@model IEnumerable<BlogPost>

@foreach(var b in Model)
{
    @Html.ActionLink(b.Title, "SingleBlogPostView", "BlogController", new { @id = b.ID })
    @Html.Display(b.Timestamp.ToString())  
    <br />
    @Html.ActionLink("Edit Post", "Edit", new { ID = b.ID })
    <br /><br />
}